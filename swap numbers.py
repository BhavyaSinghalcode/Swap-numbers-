# Given inputs
x = 5
y = 9
z = 1

# Creating a list of the variables
numbers = [x, y, z]

# Sorting the list in ascending order
numbers.sort()

# Assigning the sorted values back to x, y, z
x, y, z = numbers

# Printing the updated values
print(f"x = {x}, y = {y}, z = {z}")
